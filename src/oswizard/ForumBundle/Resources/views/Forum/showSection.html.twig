{% extends "oswizardForumBundle::layout.html.twig" %}

{% block title %}
{{section.name}}
{% endblock %}

{% block subtitle %}
{{parent()}}- {{section.name}}
{% endblock %}

{% block forum_body %}
<ul>
    {% for post in posts %}
    <li>
        <a href="{{path('oswizard_forum_show_post', {'idPost':post.id})}}">{{post.title}}</a>
    </li>
    {% else %} <li>No posts to show</li>
    {% endfor %}
</ul>
<a href="{{ path('oswizard_forum_add_post', {'idSection':section.id}) }}" class="btn">
    add post
</a>
{% endblock %}
